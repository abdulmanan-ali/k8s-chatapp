apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: chatapp 
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
      - name: frontend
        image: abdulmanan46/k8s-chat-app-frontend:latest
        ports:
        - containerPort: 80
